#include <bits/stdc++.h>

using namespace std;

int main()

{
    int u1; // first task
    int u2; // second task
    int u3; // third task

    cin >> u1 >> u2 >> u3;

    if ( u1 != 0 && u2 != 0 && u3 != 0)
    {
        cout << "Þilvinas Eglæ namø aplankyti iðleis" << endl;
    }
    else if ( u1 == 0 || u2 == 0 || u3 == 0)
    {
        cout << "Þilvinas Eglës aplankyti namø neiðleis" << endl;
    }

    return 0;
}



